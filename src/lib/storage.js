// Logo upload functionality is intentionally removed.
export const MAX_LOGO_SIZE = 0;

export async function uploadBusinessLogo() {
  throw new Error('Business logo upload is disabled for now.');
}

export default uploadBusinessLogo;
